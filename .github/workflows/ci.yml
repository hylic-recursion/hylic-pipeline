name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: hylic-recursion/.github/.github/workflows/rust-ci.yml@master
    with:
      test: |
        cargo test
